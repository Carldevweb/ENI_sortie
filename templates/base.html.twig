<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/logo.png') }}" alt="Logo Sortir.com">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <link rel="icon" href="{{ asset('img/logo.png') }}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <header>
        <div class="container_header">
            <div class="row">
                <div class="col-3">
                    <h1 class="header-logo">
                        <a href="{{ path('home') }}" title="A Web Page">
                            <img width="100px" height="100px" src="{{ asset('img/logo.png') }} " alt="Logo Sortir.com">
                        </a>
                    </h1>
                </div>

                <div class="col-6" style="direction: rtl">
                    <ul class="nav navbar-nav">
                        <a class="navbar-brand" href="app_dev.php">Villes</a>
                        <a href="app_dev.php/contact">campus</a>
                        <a href="app_dev.php/liens">Accueil</a>
                        <a href="app_dev.php/liens">Mon Profil</a>
                        <a href="app_dev.php/liens">Se déconnecter</a>
                    </ul>
                </div>
                <div class="col-3" style="direction: rtl">
                    Date de jour : {{ "now"|date("m/d/Y") }}
                    Participant : {{ 'user1' }}
                </div>
            </div>
        </div>
    </header>
    <main>
        <div class="container">
            {% block body %}

            {% endblock %}
        </div>
    </main>
    {% block footer %}
        <div class="container"></div>
    {% endblock %}
    </body>
</html>
